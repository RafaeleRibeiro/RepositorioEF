@model IEnumerable<EFExemplo.Models.Cliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Cadastro de Clientes</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Adicionar</a>
</p>
<table class="table table-striped">
    <thead>
        <tr class="table-striped">
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataNascimento)
            </th>
            <th></th>
            <th></th>
            <th></th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataNascimento)
                </td>
                <td>
                    <a asp-action="Edit" class="btn btn-outline-warning" asp-route-id="@item.CLienteId">
                        <svg bootstrap-icon="PencilFill" width="24" height="16" aria-label="Editar"></svg>
                    </a>
                </td>
                <td>
                    <a asp-action="Details" class="btn btn-outline-info" asp-route-id="@item.CLienteId">
                        <svg bootstrap-icon="CardList" width="24" height="16" aria-label="Detalhes"></svg>
                    </a>
                </td>
                <td>
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.CLienteId">
                        <svg bootstrap-icon="Trash" width="24" height="16" aria-label="Alarms"></svg>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
